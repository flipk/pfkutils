
alias lapd='export DISPLAY=laptop:0'
alias bld='export DISPLAY=blade:0'
alias bl='rsh -l flipk blade'

xconsole_hook() {
    xhost blade
    xhost runner
}

tty=`tty`
if [[ $tty != ${tty#/dev/ttyv} ]] ; then
    unset -f x
    x() {
        clear
        xinit > .xinitlog 2>&1
    }
fi
