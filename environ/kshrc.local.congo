
xconsole_hook() {
    sleep 1
    xterm -g +504+400 -e lg9 x &
    d1 emacs -g 80x58-0-23 login/notes/todo &
    xhost +
}

tty=`tty`
if [[ $tty != ${tty#/dev/ttyv} ]] ; then
    unset -f x
    x() {
        clear
        xinit > .xinitlog 2>&1
    }
fi

