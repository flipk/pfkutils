
package PFK.i3;

// common stuff first

enum ProtocolVersion {
  PROTOCOL_VERSION_1 = 1;
  // increment every time you modify this file
};

message ProtoVersion {
  required string app_name = 1; // "PFK_i3"
  required int32 version = 2; // PROTOCOL_VERSION_x
}

// server to client

enum ServerToClientType {
  STC_PROTO_VERSION = 1;
  //xxx
}

message ServerToClient {
  required ServerToClientType type = 1;
  optional ProtoVersion proto_version = 2;
  //xxx
}

// client to server

