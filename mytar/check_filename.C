
//
// check a file path for validity and fix any inconsistencies:
// - path must not start with "/" (strip it off)
// - path must not have any ".." (return NULL)
// - path must not have double-// (strip one of them)
//

char *
check_filename( char * in )
{
}
