
python3 -m venv venv
ln -s venv/bin/activate
. activate
pip install --upgrade pip setuptools wheel meson ninja

ubuntu:
  sudo apt install python3-dev libgirepository1.0-dev libcairo2-dev pkg-config
fedora:
  ?

(see also comments in requirements.txt on pip commands)
mkdir venv/wheelhouse
pip download -r requirements.txt -d venv/wheelhouse
pip install --no-index --find-links=venv/wheelhouse <packagename>
pip install --no-index ./venv/wheelhouse/*.whl <packagename>
