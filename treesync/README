
- walk both trees, generating list of name,size,mtime
- walk database, used as a cache of md5:  either fetching md5 (if
  size and mtime match) or calculating md5 and updating database
- if a file is new, add to the database
- if a file is in the database but doesn't exist anymore, delete from 
  database.

